import { Component, inject } from '@angular/core';import { Router } from '@angular/router';
/** * Page 1 (currently the entry page) — couple names reveal. Uses the * Bg-1.png artwork as the backdrop (shown instantly, no fade, so it * doesn't flash), with the names/heart staggering in via CSS * animation-delay. `router` is kept injected for navigating forward * once the next page exists. */@Component({  selector: 'app-names',  standalone: true,  templateUrl: './names.component.html',  styleUrl: './names.component.css'})export class NamesComponent {  private router = inject(Router);}
